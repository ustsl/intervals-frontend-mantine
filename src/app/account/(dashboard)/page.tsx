import { ListWidget } from "@/components/widgets/ListWidget";

export default function Dashboards() {
    return (
        <ListWidget anchor={"dashboard"} title={"Дашборды"} crTitle={"Создать дашборд"} />
    );
}
